version: 2
jobs:
  build:
    docker:
      - image: node:alpine
    steps:
      - checkout
      - run:
          name: Install dependancies
          command: npm install
      - run:
          name: Run tests
          command: npm test

workflows:
  version: 2
  build_deploy:
    jobs:
      - build
